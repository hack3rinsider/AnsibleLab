---
 - name: Handler Demo
   hosts: web1
   become: yes

   tasks:
    - name: Copy index file
      copy:
        src: ./index.html
        dest: /var/www/html/index.html
      notify: restart nginx

   handlers:

    - name: restart nginx
      service:
          name: nginx
          state: restarted
