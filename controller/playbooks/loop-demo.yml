---
- name: Loop demo
  hosts: web
  become: yes

  tasks:

    - name: Install multiple packages
      apt:
        name: "{{ item }}"
        state: present

      loop:
        - vim
        - htop
        - tree
        - curl
